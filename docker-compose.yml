version: '3'

services:
  web:
    build: .
    env_file: ./.env
    command: bash -c "python manage.py --bind 0.0.0.0:8003 numerical.wsgi"
    image: numerical
    container_name: numerical
    volumes:
      - .:/numerical
    ports:
      - "8003:8003"
    restart: unless-stopped